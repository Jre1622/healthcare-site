{
  "name": "healthcare-site",
  "version": "1.0.0",
  "description": "Local Health care",
  "main": "index.js",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -c ./tailwind.config.js -i ./css/input.css -o ./css/styles.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^4.1.5"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.5"
  }
}
